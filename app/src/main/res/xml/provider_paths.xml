<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path name="external_files" path="."/>

    <!-- For sharing files to other apps. For instance, share images to whatsapp -->
    <files-path name="shared-images" path="shared-images/" />

</paths>